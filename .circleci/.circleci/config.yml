version: 2.1
jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Run Tests
          command: mvn test
workflows:
  main:
    jobs:
      - build-and-test
